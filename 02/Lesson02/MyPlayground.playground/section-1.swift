// Playground - noun: a place where people can play

import Cocoa

var str = "Hello, playground"

func ageValidation {
    if ageInput.description > 21
    return "You can drink"
    if ageInput.description > 18
    return "You can vote"
    if ageInput.description > 16
    return "You can drive"
}